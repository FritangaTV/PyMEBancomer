pymeapp.controller('tipsCtrl', ['$scope', function ($scope) {
        $scope.tips = [];
        $scope.$watch('prediccion', function() {
        	if(typeof $scope.predicciones[0] !== 'undefined'){
        		
        	}
    	});
    }
]);